name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'
          bundler-cache: true

      - name: Build gem
        run: gem build howdy-jekyll-theme.gemspec

      - name: Push gem
        env:
          GEM_HOST_API_KEY: ${{ secrets.GEM_HOST_API_KEY }}
        run: gem push howdy-jekyll-theme-*.gem
        # If the API key requires OTP, set GEM_HOST_OTP secret and uncomment below:
        # run: gem push howdy-jekyll-theme-*.gem --otp ${{ secrets.GEM_HOST_OTP }}

      - name: Create release
        uses: softprops/action-gh-release@v3
        with:
          generate_release_notes: true
          files: howdy-jekyll-theme-*.gem
